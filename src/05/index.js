const raio = 1;

const comprimento = 2 * Math.PI * raio;

const area = Math.PI * (raio ** 2);

console.log("Comprimento do círculo: " + comprimento.toFixed(1));
console.log("Área do círculo: " + area.toFixed(2));
